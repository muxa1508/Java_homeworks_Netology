### «адание
¬ этой задаче мы будем писать симул€тор аттракциона.

 аждый человек, который посетил парк аттракционов, должен описыватьс€ вашим 
классом `Person. ѕро каждого человека известно его им€, фамили€ и количество билетов.

—оздайте в `Main` статический метод `List<Person> generateClients() {`, 
который бы возвращал список из пор€дка 5 людей, который вы заполн€ете пр€мо в коде 
(спрашивать пользовател€ что-то вводить в этой задаче не нужно). 
ѕридумайте данные по-разнообразнее - т.е. чтобы не звали их всех одинаково и чтобы не было у всех по одному билету.

ƒалее, в `main`, создайте очередь на аттракцион и заполните еЄ людьми из списка, который вам вернЄт метод `generateClients()`.

“еперь заведите бесконечный цикл, который делает итерации пока очередь не пуста. 
Ќа каждой итерации вытаскиваетс€ из очереди следующий клиент, выводитс€ на экран сообщени€ 
вида `јлексей Ќетоложко прокатилс€ на аттракционе`. ѕри этом у клиента уменьшаетс€ количество билетов на `1` и, если оно 
не стало нулевым, он вставл€етс€ обратно в очередь в еЄ конец.

¬ итоге у вас должно быть два класса: `Person` и `Main` со всей логикой работы.